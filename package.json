{
  "name": "recipe-workspace",
  "private": true,
  "scripts": {
    "dev": "bash -lc 'trap \"kill 0\" EXIT; npm run dev:backend & npm run dev:frontend & wait'",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev"
  }
}
